<Page x:Class="PasswordManagerWPF.Views.AddPasswordPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PasswordManagerWPF.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddPasswordPage">

    <Grid>
        <StackPanel Margin="30" MaxWidth="400" HorizontalAlignment="Left">
            <TextBlock Text="Додати новий пароль" FontSize="20" Margin="0,0,0,20" FontWeight="Bold"/>

            <Label Content="Назва сервісу (сайту):"/>
            <TextBox Text="{Binding Site, UpdateSourceTrigger=PropertyChanged}" Height="30" Margin="0,0,0,10"/>

            <Label Content="Логін / Email:"/>
            <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" Height="30" Margin="0,0,0,10"/>

            <Label Content="Пароль:"/>
            <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Height="30" Margin="0,0,0,20"/>

            <Button Content="Зберегти у сейф" Height="40" Background="#2980B9" Foreground="White" Click="btnSave_Click"/>
        </StackPanel>
    </Grid>
</Page>